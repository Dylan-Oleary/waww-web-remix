import { createStyles } from "@mantine/core";

export const useStatStyles = createStyles((theme) => ({
    label: {
        fontSize: "1em",
        margin: 0
    },
    value: {
        fontSize: "0.875em",
        margin: 0
    }
}));
